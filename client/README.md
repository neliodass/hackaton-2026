# Update Client

Aplikacja desktopowa cross-platform (Windows + Linux) w Pythonie z GUI Tkinter.

## MVP CLI update check

```bash
pip install -r requirements.txt
python src/main.py --server-url http://127.0.0.1:8000 --local-version 1.0.0
```
