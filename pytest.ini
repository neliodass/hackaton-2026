[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    crypto:   testy kryptograficzne (Ed25519, ML-DSA)
    cert:     weryfikacja certyfikatu podpisującego
    merkle:   drzewo Merkle i transparency log
    protocol: logika protokołu (freeze, downgrade, hash)
